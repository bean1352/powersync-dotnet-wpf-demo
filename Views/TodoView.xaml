<Page x:Class="PowersyncDotnetTodoList.Views.TodoView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:PowersyncDotnetTodoList.ViewModels"
      Title="To-Do Items">
    
    <Grid>
        <!-- <StackPanel Margin="10">
            <TextBox Text="{Binding NewTaskName, UpdateSourceTrigger=PropertyChanged}"
                     Width="300"
                     Height="30"/>
            <Button Content="Add"
                    Command="{Binding AddTodoCommand}"
                    CommandParameter="{Binding NewTaskName}"
                    Width="100"
                    Height="30"
                    Margin="0,5,0,10"/>
            
            <ListBox ItemsSource="{Binding Todos}"
                     SelectedItem="{Binding SelectedTodo, Mode=TwoWay}"
                     Width="500"
                     Height="300">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding Completed}"
                                      Command="{Binding DataContext.MarkCompleteCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                      CommandParameter="{Binding}"/>
                            <TextBlock Text="{Binding Name}" Width="350"/>
                            <Button Content="Delete"
                                    Command="{Binding DataContext.DeleteTodoCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}"
                                    Width="80"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel> -->
    </Grid>
</Page>
